/**
 * ImageManager module
 *
 * Provides components and utilities for managing product images with support for
 * upload, reordering, deletion, and alt text editing.
 */

export { default } from './ImageManager';
export { default as ImageManager } from './ImageManager';
export type { ImageManagerProps, ImageManagerHandle, ImageItem } from './types';
