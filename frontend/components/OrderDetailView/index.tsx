/**
 * OrderDetailView Component - Barrel Export
 *
 * Main entry point for the OrderDetailView component.
 * Exports the component and its types.
 */

export { default } from './OrderDetailView';
export type { OrderDetailViewProps, OrderState } from './types';
