# API Configuration
# Backend API URL - must match the backend server URL
NEXT_PUBLIC_API_URL=http://localhost:3001

# App Configuration
NEXT_PUBLIC_DEFAULT_LOCALE=vi
NEXT_PUBLIC_SUPPORTED_LOCALES=vi,en
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# OAuth Authentication
# The frontend uses OAuth-only authentication (Google and Facebook).
# OAuth credentials must be configured in the backend (backend/.env).
# See OAUTH_SETUP.md in project root for complete setup instructions.
